# allow system_app to interact with pasr hal
hal_client_domain(system_app, hal_pasrmanager)

# allow the system app search the fingerprint
allow system_app hal_fingerprint_hwservice:hwservice_manager find;
